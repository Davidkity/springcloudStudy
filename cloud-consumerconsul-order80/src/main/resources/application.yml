server:
  port: 80

spring:
  application:
    name: consul-consumer-order
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        instance-id: consulOrder80  #设置服务名称
        prefer-ip-address: true  #显示服务的IP地址